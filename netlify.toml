# Netlify Configuration for Frontend
[build]
  publish = "frontend"

# Redirect API calls to backend (Vercel)
[[redirects]]
  from = "/api/*"
  to = "https://college-eventweb.vercel.app/api/:splat"
  status = 200
  force = true

# SPA-style routing — serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
